1. run dockerfile
https://docs.docker.com/engine/examples/running_ssh_service/#build-an-eg_sshd-image
http://phase2.github.io/devtools/common-tasks/ssh-into-a-container/
https://medium.com/@furkanpur/remote-python-debug-to-docker-container-over-ssh-by-using-pycharm-44a9b6e82206
$ docker build -t eg_sshd .
$ docker run -d -P --name test_sshd eg_sshd
$ docker port test_sshd 22

2. find ip, either log into docker and get ip there
docker exec -it test_sshd /bin/bash
hostname -i

or insepct the docker container from outside
docker inspect test_sshd | grep "IPAddress"

3. log into docker and verify the credential by running 
docker exec -it test_sshd /bin/bash
ssh root@172.17.0.2 -p 32774


172.17.0.2:32772
user name: root
passowrd: screencast
root@99af899ad1fc:/#

try to ssh within the container



3. install https://www.putty.org/ testing the credential

4. clean up
$ docker container stop test_sshd
$ docker container rm test_sshd
$ docker image rm eg_sshd


1. remove all containers
docker rm $(docker ps -a -q)

2. build image
3. run container
4. get ip and get port
5. visual studion remote debug 
tcp://my_secret@127.0.0.1:32770/
